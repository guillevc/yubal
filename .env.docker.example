# Server
YUBAL_HOST=0.0.0.0
YUBAL_PORT=8000
YUBAL_RELOAD=false
YUBAL_DEBUG=false

# Paths
YUBAL_DATA_DIR=/app/data
YUBAL_BEETS_DIR=/app/beets
YUBAL_YTDLP_DIR=/app/ytdlp

# Audio (opus = best quality/size, no transcoding; options: opus, m4a, mp3, flac, best)
YUBAL_AUDIO_FORMAT=opus
YUBAL_AUDIO_QUALITY=0
